os: linux
cache:
  - apt

language: generic
matrix:
  include:
  - name: "Bionic melodic"
    dist: bionic
    env: ROS_DISTRO=melodic
  - name: "Xenial kinetic"
    dist: xenial
    env: ROS_DISTRO=kinetic

env:
  global:
    - ROS_CI_DESKTOP="`lsb_release -cs`"
    - CATKIN_OPTIONS=$TRAVIS_BUILD_DIR/bindings/ros/catkin.options
    - ROS_PARALLEL_JOBS='-j8 -l6'

before_install:
    - sudo sh -c "echo \"deb http://packages.ros.org/ros/ubuntu $ROS_CI_DESKTOP main\" > /etc/apt/sources.list.d/ros-latest.list"
    - sudo apt-key adv --keyserver 'hkp://keyserver.ubuntu.com:80' --recv-key C1CF6E31E6BADE8868B172B4F42ED6FBAB17C654
    - sudo apt-get update -qq
    - sudo apt-get install -y python-catkin-pkg python-rosdep python-wstool ros-$ROS_DISTRO-ros-base 
    - if [ "$ROS_EXAMPLES" ];  sudo apt-get install -y ros-$ROS_DISTRO-rviz ; fi
    - source /opt/ros/$ROS_DISTRO/setup.bash
    - sudo rosdep init
    - rosdep update

install:
  - mkdir -p ~/catkin_ws/src
  # Create the devel/setup.bash (run catkin_make with an empty workspace) and
  # source it to set the path variables.
  - cd ~/catkin_ws
  - source /opt/ros/$ROS_DISTRO/setup.bash
  - catkin_make
  - source devel/setup.bash
  # Add the package under integration to the workspace using a symlink.
  - cd ~/catkin_ws/src
  - ln -s $TRAVIS_BUILD_DIR/bindings/ros/aditof_roscpp .

script:
  - source /opt/ros/$ROS_DISTRO/setup.bash
